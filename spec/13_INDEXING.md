# Index Strategy

Index:
organization.code
site.organization_id
building.site_id
floor.building_id
grid.floor_id
cell.grid_id
campaign.floor_id
fingerprint.cell_id
fingerprint.campaign_id
observation.fingerprint_id
model.dataset_id