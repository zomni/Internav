# Android Applications

The platform contains two independent Android applications.

1. Capture App
2. End User App

Both applications share common libraries where possible but remain independently deployable.