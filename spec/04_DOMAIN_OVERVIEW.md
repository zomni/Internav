# Domain Overview

The platform is domain-independent.

The hierarchy is:

Organization
└── Site
    └── Building
        └── Floor
            ├── FloorPlan
            ├── Grid
            │   └── Cell
            └── Campaign
                └── Fingerprint

Training produces one or more Model Versions based on captured datasets.